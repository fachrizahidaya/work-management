import { Flex, Text } from "native-base";
import React from "react";

const ProjectTaskAttachmentItem = ({
  bandAttachmentType,
  id,
  project_id,
  project_title,
  owner_id,
  owner_name,
  owner_email,
  description,
  title,
  project_no,
  task_no,
}) => {
  return <Text>tes</Text>;
};

export default ProjectTaskAttachmentItem;
